#ifndef MATRIX_H
#define MATRIX_H

char** assignMatrix(int rows, int columns);
void freeMatrix(char** input, int rows);
void displayMatrix(char** input, int rows, int columns);
/*char** fillMatrix(char** input, int rows, int columns);*/

#endif
